{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Point of view",
  "type": "object",
  "additionalProperties": false,
  "required": ["actor", "position", "reasoning", "evidence", "external_check", "mode", "movement"],
  "properties": {
    "actor": {
      "type": "string",
      "enum": ["ai:assistant"],
      "description": "Neutral actor identity."
    },
    "position": {
      "type": "string",
      "minLength": 1,
      "description": "The adoption grade, document or approach bottom line, skeptic verdict, or blocked state."
    },
    "reasoning": {
      "type": "string",
      "minLength": 1,
      "description": "Concise reasoning for the position, including the reason when blocked."
    },
    "evidence": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "A succinct claim with a URL, file:line, or named document section citation."
      }
    },
    "external_check": {
      "type": "string",
      "enum": ["ran", "unavailable"]
    },
    "mode": {
      "type": "string",
      "enum": ["independent", "skeptic"]
    },
    "movement": {
      "type": "string",
      "enum": ["initial", "moved", "held"],
      "description": "Initial position, or whether the peer moved or held during reconciliation."
    },
    "cross_model_route": { "type": "string" },
    "cross_model_target": { "type": "string" },
    "serving_family": { "type": "string" },
    "independence_verified": { "type": "boolean" }
  }
}
