# fixture: cross-model peer folded

`run/` is a ce-code-review run directory after Stage 4's local reviewers finished. The peer job completed and wrote `adversarial-codex.json` with `independence_verified: true`.
