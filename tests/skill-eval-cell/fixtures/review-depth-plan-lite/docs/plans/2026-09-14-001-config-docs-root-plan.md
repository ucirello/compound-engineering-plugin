# Plan: declare docs_root in CE config

## Product Contract

### Requirements

- R1: `.compound-engineering/config.yaml` declares `docs_root: docs`.
- R2: `README.md` gains a one-line note that the CE artifact root is `docs/`.

## Implementation Units

### U1. Add the config key (R1)

### U2. Document the artifact root in README (R2)
