Internal export service. Authenticated account owners can obtain signed download links for existing private exports. The upstream request adapter supplies a validated account ID and export ID. Use the existing ownership helper before signing. Storage and signing are injected functions; no provider class, retry policy, persistence change, or retention job belongs in this change. Missing exports return 404; cross-account requests return 403. Tests run with node --test.
