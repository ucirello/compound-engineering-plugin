# fixture: cross-model peer failed

`run/` is a ce-code-review run directory after Stage 4's local reviewers finished. The peer job under `run/jobs/` ended `failed` with no `adversarial-codex.json`; its `out.log` carries the peer skip evidence.
