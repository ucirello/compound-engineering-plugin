import type { Scenario } from "./catalog"

const baseline_ref = "988eb4008e24e7c5cde2fc4a6141c6baf9e8f560"
const fixture = "tests/skill-eval-cell/fixtures/review-calibration"
const base = { cohort: "resized", key_behavior: "judgment", read_only: true, fixture, baseline_ref } as const

export const CALIBRATION_SCENARIOS: Scenario[] = [
  {
    ...base,
    id: "ce-doc-review/rejected-evidence-freshness",
    skill: "ce-doc-review",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    why: "An unchanged document quote must not keep a prior rejection binding after material source changes.",
    pre_contract: "R29 suppression matches section, title and evidence overlap; its changed-context exception describes document edits.",
    task: `Use ce-doc-review for R29 reconciliation only. Read references/document-intake.md and references/synthesis-and-presentation.md. A fresh review has completed. For each candidate, return RECONCILIATION: A=<suppress|reassess>, B=<suppress|reassess>, and explain whether reassessment grants permission to reverse an existing user decision. Do not dispatch or edit.

Both findings match a prior skipped finding's normalized title and section, and repeat its document quote verbatim. A concerned duplicate billing under retries. It was previously rejected because the source never retried. The unchanged plan still says "Send the charge request." Source code has now changed to retry charge requests after timeout, and the reviewer demonstrated two charges when the first request succeeded before timing out. B suggests renaming a clearly defined term for consistency. It was rejected as a preference. Its document, source, requirements, and evidence are unchanged.`,
    grade: { must_include_field: "RECONCILIATION", must_include: ["A=reassess", "B=suppress"], actions: "none", delegates: "none" },
  },
  {
    ...base,
    id: "ce-doc-review/retained-output-counts",
    skill: "ce-doc-review",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    why: "Interactive output must not count rejected raw residuals or invent deferred questions from template examples.",
    pre_contract: "The template calls counts post-synthesis but explicitly derives Residual from raw persona output and illustrates an unsupported deferred API question.",
    task: `Use ce-doc-review to render only Deferred Questions and Coverage from a completed synthesis result. Read references/review-output-template.md. There is one completed feasibility reviewer, zero findings, no fixes, no FYIs, and no reviewer failure. Its raw return contained two residual risks about hypothetical scale and a suggestion to version API endpoints, all rejected by synthesis as unsupported. The retained review has zero residual concerns and one deferred question: the support team must choose whether customers are promised 7 or 30 days of export recovery; neither commitment is settled and research cannot choose for the team. Render the retained sections. Do not dispatch, edit, or ask the question now.`,
    grade: { must_include: ["7", "30", "Coverage"], actions: "none", delegates: "none" },
  },
  {
    ...base,
    id: "ce-doc-review/fyi-template-admission",
    skill: "ce-doc-review",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    why: "The interactive template illustrated filename preferences and unsupported thresholds as FYIs despite synthesis admission.",
    pre_contract: "The output template includes filename asymmetry and speculative monitoring thresholds as anchor-50 observations.",
    task: `Use ce-doc-review to finish only advisory admission and the interactive FYI presentation for completed review candidates. Read references/synthesis-and-presentation.md and references/review-output-template.md. Do not dispatch, edit documents, or run the full review. Return the FYI section and its retained count.

All candidates have confidence 50. A coherence reviewer prefers renaming plan.md to match the command name, while confirming neither name misleads readers. A scope reviewer suggests monitoring thresholds for hypothetical future traffic, with no traffic measurements or deployment requirement supporting that concern. A feasibility reviewer verified recorded manual fixture setup takes 15 minutes per run; the existing seed script produces the same fixture in 2 minutes. Both setup methods already satisfy the plan, and this optional convenience is below the actionable bar.`,
    // The retained fact is the 15-versus-2-minute comparison, however phrased.
    grade: { must_include_any: [["15 minutes", "15 min", "versus 15", "15 for"], ["2 minutes", "2 min"]], actions: "none", delegates: "none" },
  },
  {
    ...base,
    id: "ce-code-review/settled-preference-admission",
    skill: "ce-code-review",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    why: "A settled-conflict marker previously forced preference-only feedback back into the primary report.",
    pre_contract: "Synthesis retains preferences against settled decisions as report-only findings even when suppressed by the helper.",
    task: `Use ce-code-review at Stage 5 settlement reconciliation and admission. Read references/finish-review.md. Resolve only the routing of these completed reviewer candidates; do not dispatch, edit files, or run the full review.

The discovered plan explicitly states: K1 (session-settled: keep function-based injection, rejected a provider class hierarchy). Account isolation is required: only the owning account can receive a signed download URL. Reviewers verified the implementation calls sign(record.id) without checking accountId against record.accountId.

Candidate A: P2, confidence 50, settled_conflict K1. Suggest a provider class hierarchy instead of the chosen function-based injection. Both designs satisfy all requirements; the reviewer prefers classes for symmetry. The helper suppressed it by confidence.
Candidate B: P1, confidence 100. The missing ownership check lets another account obtain a private export URL. Evidence: return { url: await sign(record.id) }. Fix by calling the existing assertOwner before sign; the function-based design remains intact.

Declare each routing on its own line, exactly one line per candidate: \`CANDIDATE A: <discard|advisory|actionable|user-decision>\` and \`CANDIDATE B: <...>\`. Then explain the reason for each and whether local apply would be authorized by a review-only invocation. Stop there.`,
    grade: { declared: { "CANDIDATE A": "discard", "CANDIDATE B": "actionable" }, actions: "none", delegates: "none" },
  },
  {
    ...base,
    id: "ce-code-review/live-review",
    skill: "ce-code-review",
    fixture: "tests/skill-eval-cell/fixtures/code-review-live",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    read_only: false,
    key_behavior: "delegation",
    git_init: true,
    git_staged: ["src/endpoint.js", "endpoint.test.js"],
    timeout_secs: 900,
    why: "Run real reviewers against a private-download defect while checking restraint on explicitly excluded work.",
    pre_contract: "Code review reports findings for caller-owned application without blocking prompts.",
    task: `Use ce-code-review mode:agent base:HEAD plan:PLAN.md to review the staged endpoint change. Run the actual reviewer workflow and retain the review artifacts. The staged implementation and tests belong to this task. Do not apply fixes, commit, publish, or create tickets.`,
    grade: { workspace_read: ["PLAN.md", "src/endpoint.js"], delegates: "some" },
  },
  {
    ...base,
    id: "ce-plan/completed-review-handoff",
    skill: "ce-plan",
    fixture: "tests/skill-eval-cell/fixtures/doc-review-caller",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    read_only: false,
    key_behavior: "judgment",
    git_init: true,
    why: "Resume planning with actual review output and measure which concerns the caller forwards or resolves.",
    pre_contract: "The planner passes returned findings through verbatim and derives menu counts from the original envelope.",
    task: `Continue ce-plan after its document review. PLAN.md is the draft you authored under my request to plan the CSV download described in README.md; the confidence check is complete. REVIEW.md is the completed review result. Finish the planning handoff using these artifacts. Do not dispatch a new review, implement production code, commit, or publish.`,
    grade: { workspace_read: ["PLAN.md", "REVIEW.md"], delegates: "none" },
  },
  {
    ...base,
    id: "ce-doc-review/advisory-admission",
    skill: "ce-doc-review",
    fixture: "tests/skill-eval-cell/fixtures/doc-review-advisory",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    why: "Check actual reviewer generation for harmless preferences, real contradictions, and rejected claims leaking into residual fields.",
    pre_contract: "Persona and shared rubrics retain harmless preferences as advisory observations.",
    task: `Review PLAN.md as the coherence-reviewer leaf in ce-doc-review. Read the bundled coherence-reviewer persona, subagent-template.md and findings schema. Apply that reviewer contract and return its JSON findings. Do not dispatch other reviewers or edit files.`,
    grade: { workspace_read: ["PLAN.md"], actions: "none", delegates: "none" },
  },
  {
    ...base,
    id: "ce-doc-review/live-review",
    skill: "ce-doc-review",
    fixture: "tests/skill-eval-cell/fixtures/review-live",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    read_only: false,
    key_behavior: "delegation",
    git_init: true,
    why: "Exercise live reviewer dispatch, synthesis, edits, and the first approval boundary on a source-backed plan.",
    pre_contract: "Document review generates findings through native personas and gates semantic changes for approval.",
    task: `Use ce-doc-review to review PLAN.md. You may inspect source and dispatch real reviewer subagents. Keep reviewer returns in this workspace so the review can be inspected later. Do not publish, commit, or modify production source.`,
    grade: { workspace_read: ["PLAN.md"], delegates: "some" },
  },
  {
    ...base,
    id: "ce-doc-review/default-edit-authority",
    skill: "ce-doc-review",
    fixture: "tests/skill-eval-cell/fixtures/doc-review-edit-authority",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    read_only: false,
    key_behavior: "mutation",
    git_init: true,
    why: "Distinguish permission to correct verification from permission to choose product behavior; reject redundant clarification work.",
    pre_contract: "Only mechanical corrections apply automatically under default review authority.",
    task: `Continue ce-doc-review mode:non-interactive at synthesis for PLAN.md using the completed local reviewer returns in REVIEW.json. The document is unchanged since those reviews. Inspect the document and handle the results through completion, including any edits the skill permits. Do not dispatch another review or modify production code.`,
    grade: { workspace_read: ["PLAN.md", "REVIEW.json"], delegates: "none" },
  },
  {
    ...base,
    id: "ce-doc-review/report-only-edit-authority",
    skill: "ce-doc-review",
    fixture: "tests/skill-eval-cell/fixtures/doc-review-edit-authority",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    read_only: false,
    key_behavior: "mutation",
    git_init: true,
    why: "Distinguish permission to correct verification from permission to choose product behavior; reject redundant clarification work.",
    pre_contract: "Only mechanical corrections apply automatically under default review authority.",
    task: `Continue ce-doc-review mode:non-interactive at synthesis for PLAN.md using the completed local reviewer returns in REVIEW.json. The document is unchanged since those reviews. Inspect the document and handle the results through completion, including any edits the skill permits. This review is report-only: do not edit PLAN.md or any production file. Do not dispatch another review.`,
    grade: { workspace_read: ["PLAN.md", "REVIEW.json"], actions: "none", delegates: "none" },
  },
  {
    ...base,
    id: "ce-doc-review/default-authority-broad-goal",
    skill: "ce-doc-review",
    fixture: "tests/skill-eval-cell/fixtures/doc-review-commitment-boundary",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    read_only: false,
    key_behavior: "mutation",
    git_init: true,
    why: "A broad privacy goal does not authorize adding a new expiration policy.",
    pre_contract: "Only mechanical corrections apply automatically under default review authority.",
    task: `Continue ce-doc-review mode:non-interactive at synthesis for PLAN.md using the completed local reviewer returns in REVIEW.json. The document is unchanged since those reviews. Inspect the document and handle the results through completion, including any edits the skill permits. Do not dispatch another review or modify production code.`,
    grade: { workspace_read: ["PLAN.md", "REVIEW.json"], actions: "none", delegates: "none" },
  },
  {
    ...base,
    id: "ce-doc-review/granted-edit-authority",
    skill: "ce-doc-review",
    fixture: "tests/skill-eval-cell/fixtures/doc-review-edit-authority",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    read_only: false,
    key_behavior: "mutation",
    git_init: true,
    why: "Distinguish permission to correct verification from permission to choose product behavior; reject redundant clarification work.",
    pre_contract: "Only mechanical corrections apply automatically under default review authority.",
    task: `Continue ce-doc-review mode:non-interactive at synthesis for PLAN.md using the completed local reviewer returns in REVIEW.json. The document is unchanged since those reviews. Inspect the document and handle the results through completion, including any edits the skill permits. Do not dispatch another review or modify production code. I authorize corrections to the implementation and verification sections needed to satisfy the existing Product Contract. Preserve the Product Contract and its unresolved choices.`,
    grade: { workspace_read: ["PLAN.md", "REVIEW.json"], delegates: "none" },
  },
  {
    ...base,
    id: "ce-doc-review/ownership-transcript",
    skill: "ce-doc-review",
    fixture: "tests/skill-eval-cell/fixtures/doc-review-ownership",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    why: "Replay a real rendered review with excessive human decisions against its pre-review plan.",
    pre_contract: "Reasonable alternative remedies and peer-only manual findings require individual user decisions.",
    task: `Continue ce-doc-review at synthesis for PLAN.md using the completed review in REVIEW.md. Reassess every proposed fix, numbered decision, and FYI against the plan. Return each disposition, its evidence, and whether it belongs in applied changes, grouped confirmation, user decisions, or is dropped. The review is a rendered transcript, not raw persona JSON; preserve known provenance and do not invent missing confidence or corroboration. Stop before interaction, edits, or dispatch.`,
    grade: { workspace_read: ["PLAN.md", "REVIEW.md"], actions: "none", delegates: "none" },
  },
  {
    ...base,
    id: "ce-doc-review/ownership-boundary",
    skill: "ce-doc-review",
    fixture: "tests/skill-eval-cell/fixtures/doc-review-ownership-boundary",
    baseline_ref: "153e605e1622154a0d7da095fceed13edcb68bf7",
    why: "Distinguish an in-scope engineering remedy from an unapproved user commitment, including peer-only provenance.",
    pre_contract: "Peer-only manual findings stay on the decision surface even when local inspection settles their remedy.",
    task: `Continue ce-doc-review at synthesis for PLAN.md. A cross-model peer returned two manual findings at confidence 100: E1 proposes replacing the duplicated ownership condition in src/endpoint.js with assertOwner, citing src/export.js; retaining the equivalent inline guard is also viable. E2 proposes adding automatic deletion after 7 days; 30 days is also viable. An in-process reviewer independently confirmed E2's omission but did not review E1. Inspect the evidence and decide whether each finding survives, what remedy you recommend, and how it routes. Stop before interaction, edits, or dispatch.`,
    grade: { workspace_read: ["PLAN.md", "src/endpoint.js", "src/export.js"], actions: "none", delegates: "none", must_include: ["retention"] },
  },
  {
    ...base,
    id: "ce-doc-review/calibration-adjudication",
    skill: "ce-doc-review",
    why: "Exercise synthesis after independent reviewers returned: correlated low-impact observations, a fact-resolved disagreement, a real omission, and a product fork.",
    pre_contract: "Synthesis gates anchors, promotes agreement, combines contradictions as manual, and preserves grouped confirmation for semantic edits.",
    task: `Continue ce-doc-review at synthesis for PLAN.md. The reviewers have finished. Their findings are in REVIEW.json; source evidence is available in the workspace. Do not dispatch reviewers or edit the document. Return your final classification and proposed disposition for each finding ID and every residual risk or deferred question, with the evidence that decided it, then stop before presenting an interaction.`,
    grade: { workspace_read: ["PLAN.md", "REVIEW.json"], actions: "none", delegates: "none", must_include: ["ownership", "retention"] },
  },
  {
    ...base,
    id: "ce-doc-review/calibration-resume",
    skill: "ce-doc-review",
    why: "Resuming decisions is not a new reviewer pass; actual dispatch is outside this decision-only cell.",
    pre_contract: "Interactive re-entry reuses prior safe-auto and R29 state but normally traverses Phase 2 again.",
    task: `We just completed a non-interactive ce-doc-review of PLAN.md. The complete reviewer returns are in REVIEW.json, the document has not changed, and no decisions have been answered. I now want to handle those existing findings. Resolve the next step and report whether a new reviewer pass is needed and why. Stop at that routing decision; do not dispatch or edit.`,
    grade: { workspace_read: ["PLAN.md", "REVIEW.json"], actions: "none", delegates: "none" },
  },
  {
    ...base,
    id: "ce-pov/calibration-missing-framing",
    skill: "ce-pov",
    why: "An essential unstated product preference returns as missing context, not an interview or a confident guess.",
    pre_contract: "Approach-set positions may say Either is viable; ambiguous criteria ordinarily ask or route to brainstorm.",
    task: `Use ce-pov to choose the export retention approach in APPROACHES.md for this project.`,
    grade: { workspace_read: ["APPROACHES.md"], actions: "none", delegates: "none", must_include: ["retention"] },
  },
  {
    ...base,
    id: "ce-pov/calibration-grounded-position",
    skill: "ce-pov",
    why: "Missing incidental detail must not prevent a position already decided by verified project requirements.",
    pre_contract: "Clear approach-set intent grounds against source, returns a position, and stays read-only.",
    task: `Use ce-pov: should the download endpoint use the existing assertOwner helper before signing, or rely solely on the signed URL? This is a bounded implementation choice for the project in PLAN.md.`,
    grade: { workspace_read: ["PLAN.md", "src/export.js"], actions: "none", must_include: ["assertOwner"] },
  },
  {
    ...base,
    id: "ce-work/calibration-followup",
    skill: "ce-work",
    why: "Caller adjudication separates an authorized implementation choice from an unresolved product commitment before asking the human.",
    pre_contract: "Followup biases to act but directs fixers to skip design judgment; interactive residuals always prompt.",
    task: `We are at ce-work's review-findings followup. PLAN.md is the approved scope except its explicitly unresolved retention question. The completed review proposes two fixes: (1) replace the duplicated ownership condition in src/endpoint.js with the existing assertOwner helper, preserving behavior; (2) resolve retention by deleting exports after 7 days. Determine the disposition of each under the followup and residual-work rules. Stop before dispatch, edits, or shipping.`,
    grade: { workspace_read: ["PLAN.md", "src/endpoint.js"], actions: "none", delegates: "none", must_include: ["assertOwner", "retention"] },
  },
  {
    ...base,
    id: "ce-work/calibration-required-decision",
    skill: "ce-work",
    why: "A user-owned decision that prevents the requested outcome must remain a blocker, unlike independent nonblocking residuals.",
    pre_contract: "The old autonomous residual gate may record unsettled product decisions and continue shipping.",
    task: `We are at ce-work's Residual Work Gate in an autonomous run. The user now explicitly requires automatic deletion of stored exports as part of the completed outcome, in addition to PLAN.md's ownership work. The ownership work is done, but the retention period in APPROACHES.md remains unapproved and no criteria settle it. Decide whether to ship, record a nonblocking residual, or return a blocker. Stop at the disposition; do not dispatch, edit, or ship.`,
    grade: { workspace_read: ["APPROACHES.md"], actions: "none", delegates: "none", must_include: ["block", "retention"] },
  },
  {
    ...base,
    id: "ce-code-review/calibration-lead",
    skill: "ce-code-review",
    why: "Lead judgment must preserve a valid existing guard and reject reviewer-generated scope and abstraction work.",
    pre_contract: "Report-only, no blocking questions, concrete consequences required; synthesis owns final routing.",
    task: `Continue ce-code-review's lead judgment on src/export.js. Reviewers returned: A says download lacks an ownership check and proposes adding one; B says the injected sign function needs a new class hierarchy for future providers; C says the verified assertOwner call already enforces the required check; D asks to define the retention period as part of this change. B also returned the hypothetical provider hierarchy as a residual risk and a deferred question. PLAN.md states the intended scope. Inspect all returned claims and give the final verdict and disposition of each. This is report-only; stop after this bounded judgment, without dispatch or mutation.`,
    grade: { workspace_read: ["PLAN.md", "src/export.js"], actions: "none", delegates: "none", must_include: ["assertOwner"] },
  },
]
