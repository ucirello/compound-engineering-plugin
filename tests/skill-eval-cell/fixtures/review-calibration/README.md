# Export service

This project exports account data. `PLAN.md` is the proposed implementation;
`src/export.js` is the existing export path. Stored exports are private.
There are no prior architecture decisions beyond those recorded in the plan.
